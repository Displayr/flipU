on:
  push:
    branches: '**'

jobs:
  push:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/displayr/r-server-skinny-base:basic
      credentials:
        username: jrwishart
        password: ${{ secrets.JUSTIN_GITHUB_PAT }}
    steps:
      - name: Install stuff
        run: |
          apt-fast install -y git
        env:
          GITHUB_PAT: ${{ secrets.JUSTIN_GITHUB_PAT }}
      - name: Pull code
        uses: actions/checkout@v2
      - name: Check directory #DELETE ME!!
        run: ls
      - name: Run R CMD check and test
        uses: r-lib/actions/check-r-package@v1
          build_args: '"--no-manual", "--no-build-vignettes", "--no-resave-data"'
          args: '"--no-manual", "--no-build-vignettes"'
  on-success: #
    run: Install
